# SETUP_DOCKER_DESKTOP.md

## 目的
Docker Desktop を導入し、チュートリアル環境を起動できる状態にする。

## 手順（最小）
1) Docker Desktop をインストール  
2) 起動して「Docker Engineが動作中」になっていることを確認  
3) `docker --version` / `docker compose version` が通ることを確認

## 注意点
- 企業端末で制限がある場合は管理者に相談してください。
- WSL2 が必要な環境では先に有効化してください。

